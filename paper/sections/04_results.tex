\section{Results}

\begin{itemize}
  \item \textbf{Table 1 --- primary LOSO results:} majority baseline; raw, handcrafted, GazeMAE, MOMENT; Random Forest and MLP
  \item Table columns: representation; model; accuracy ($\Delta$ majority); balanced accuracy ($\Delta$ majority); macro-F1; ROC-AUC
  \item Table order: majority baseline; raw; handcrafted; GazeMAE; MOMENT
  \item Subject-macro estimates; valid-fold counts in caption or table note
  \item Bold: best value per metric; no overemphasis on small model differences
  \item Main result: highest accuracy; absolute and majority-baseline improvement
  \item Representation result: raw and handcrafted performance relative to frozen GazeMAE and MOMENT embeddings
  \item Model result: consistency of representation ranking across Random Forest and MLP
  \item One-class held-out-subject treatment; metric availability
  \item Persistence-check result: contextual temporal predictability; separate protocol; no direct ranking against LOSO models
  \item \textbf{Figure 2 --- selected projection:} one representation-method combination; raw q5 continuous colour scale
  \item Figure-selection criterion: clearest engagement gradient or clearest absence of label separation
  \item Figure interpretation: exploratory geometry; mixed colours as limited label separability; no causal or performance claim
\end{itemize}
