# Project memory

## 1 — Initial repository state (2026-07-10)

- Purpose: public, data-free codebase for the IS SCAI 2026 paper comparing, in fixed order, **raw**, **handcrafted features**, **GazeMAE embeddings**, and **MOMENT embeddings** on already-computed TrustMe Tobii windows; representation generation stays in `TrustME-ET-end-to-end`.
- Main question/protocol: predict the provisional primary target **q5** on unseen subjects using leakage-safe **LOSO-normalized** evaluation; preprocessing/scaling is fit on training subjects only. Compare majority baseline, RF, and MLP; report accuracy and balanced-accuracy with deltas over majority, macro-F1, and AUC (`NaN` for undefined one-class folds). Row-normalize confusion matrices with fixed `[0,1]` scale.
- Secondary/open decisions: confirm q5; decide AUC emphasis, best PCA/t-SNE/UMAP figure, whether within-subject temporal and persistence results belong in the paper, and final IS/SCAI format. These secondary protocols are disabled in the main example config.
- Repository created locally at `/home/ppg/eyetracking/ET-representations-for-IS-paper-2026`; Git history starts with `713dd63` (copied experiment/evaluation Python only), followed by `54bafc0` (analysis, safe configs, tests, README, paper outline). GitHub creation/push is handled by the user.
- Key files: `configs/experiments.example.yaml`, `configs/analysis.example.yaml`, `tobii_experiments/run_zoja_protocols.py`, `tobii_experiments/run_data_analysis.py`, `PAPER_OUTLINE.md`, and `README.md`. Paths use `${TRUSTME_DATA_ROOT}`; no participant data, outputs, weights, credentials, or sensitive server paths may be committed.
- Analysis outputs: class and subject distributions, pupil distributions, gaze density, missingness, and PCA/t-SNE/optional UMAP projections colored by class/subject; raw CSV sampling is streamed and raw windows are resampled/flattened only for visualization. Projections are illustrative, not separability evidence.
- Environment/rules: use the single `trust-me-et` conda environment and root `requirements.txt`; typed, readable Python with concise docstrings and YAML/CLI-configurable paths. Initial verification: both CLIs/configs load, synthetic suite passes (`4 passed`), and the repository contained no large/sensitive artifacts.

## 2 — Subject-tree analysis input (2026-07-10)

- Labelled per-subject exports are sufficient for current supervised/descriptive work. Analysis config now needs only `trustme_root`; code discovers every `<subject>/ml/tobii/`, strictly requires the four standard raw/features/GazeMAE/MOMENT CSVs, combines subjects in memory, and records subjects in the manifest. `paper/` is out of scope and must remain untouched.

## 3 — Paper scaffold (2026-07-10)

- paper/main.tex is now a project-specific Information Society 2026 manuscript scaffold, replacing the pasted ACM sample. It uses references.bib (not the missing sample-base.bib), has no sample teaser asset, and contains contextual red placeholder markers for author details, dataset facts, citations, and results that are not yet known.
- The scaffold follows PAPER_OUTLINE.md: Introduction, Related Work, Dataset and Preprocessing, Representations, Experimental Setup, Results, Discussion, Conclusion, and a Reproducibility appendix. The recommended first prose is the Introduction, followed by Dataset and Preprocessing once study facts are confirmed; Results and Conclusion deliberately remain result-specific placeholders.
- Real run validated 17 subjects/68 representation CSVs and completed all PCA, t-SNE, and UMAP outputs. `umap-learn==0.5.12` was added to the existing `trust-me-et` environment (already declared in repository requirements); UMAP uses explicit `n_jobs=1` with the fixed seed for reproducibility and warning-free execution.

## 4 — Four-page paper planning (2026-07-10)

- User wants a four-page English paper including references, with neither appendix nor supplementary material. The intended core is a controlled, unseen-subject comparison of raw, handcrafted, GazeMAE, and MOMENT representations for the final selected self-report target (currently q5).
- Proposed main-results columns are representation, model, accuracy and delta over majority, balanced accuracy and delta over majority, macro-F1, and AUC. Keep deltas paired at the held-out-subject/fold level before aggregation, and keep undefined AUC values explicit for one-class test folds.
- Planned descriptive figure is a compact two-panel figure: left/right pupil-size histogram plus normalized gaze-coordinate heatmap. Screen-coordinate 0--1 normalization for the heatmap is descriptive only and must use verified per-recording screen dimensions; it is distinct from model preprocessing.
- Current `loso_normalized` code does train-fold-only median imputation followed by `StandardScaler` z-scoring per representation feature, then applies those parameters to the held-out subject. The example configuration disables the optional second model-side scaler, avoiding double scaling.

## 5 — Coordinate-normalized Tobii exports (2026-07-10)

- q5 is the numerical response to “Engagement; How immersed in your work did you feel just now?” The four-page English paper will compare representations for unseen-office-worker engagement inference; its intended title is **Comparison of Eye-Tracking Representations for Engagement Inference in Office Workers**. Frozen pretrained GazeMAE and MOMENT encoders are evaluated out of the box (only a prediction head is trained), and the intended conclusion is that embeddings do not justify their cost in this setting.
- `tobii_experiments/normalize_tobii_coordinates.py` creates each subject’s `ml/tobii_coordinate_normalized/` sibling. It copies features, GazeMAE, MOMENT, and manifest CSVs byte-for-byte and normalizes all six raw gaze columns (`GazePointX/Y`, left, right) by source-recording width/height. It maps `source_file` back from `*.tsv.parquet` to the raw TSV, reads the TSV’s `display resolution: WxH` header, and validates it against `display_resolutions.yaml`; this handles subjects with multiple displays per recording rather than selecting one subject-level resolution. It intentionally does not clip out-of-screen values, so later heatmaps must restrict to `[0,1]^2` after normalization.
- The script supports CLI/YAML configuration, chunked processing, atomic raw CSV replacement, `--dry-run`, and `--overwrite`; a synthetic test verifies per-source normalization and unchanged copied files. The 17-subject production run completed to the local raw-data tree and requires an audit before experiments/analysis point their `subject_export_dir` to `tobii_coordinate_normalized`.

## 6 — Finalized q5 analysis views (2026-07-10)

- The experimental target remains the current LOSO rule: per-training-subject mean-centering, with the held-out subject centred by the global training mean. Do not replace this with participant-median labels.
- The manuscript deliberately does not mention the diagnostic global-median q5 visualization split. It states only the actual fold-specific label rule: mean-center each training participant separately and center the held-out participant using the overall training-participant mean.
- `configs/analysis.example.yaml` now reads `tobii_coordinate_normalized` and writes `results/data_analysis_q5/`. It contains only raw-q5 and binary-q5 class distributions, the normalized gaze-density map, and PCA/t-SNE/UMAP projections; no subject-coloured, pupil/violin, or missingness figures. Outputs are grouped as `<raw|binary>_q5/<pca|tsne|umap>/` for representation-by-method comparison.

## 7 — Bullet-only manuscript map (2026-07-13)

- `paper/main.tex` is now a bullet-only writing map: empty abstract; Introduction; Related Work; Methodology with Data and Eye-Tracking Representations plus Experimental Design; Results; Discussion; Conclusion. No existing prose, appendix, or Results/Discussion subsections remain.
- The outline specifies Table 1 (main LOSO representation comparison), Figure 1 (normalized gaze-density context), and one selected continuous-q5 projection as Figure 2, including their intended interpretation. The user will write all final sentences in their own style after results are final.

## 8 — Split manuscript chapters (2026-07-13)

- `paper/main.tex` now retains only the shared template, title material, empty abstract, acknowledgements, bibliography, and `\input` directives. The six manuscript chapters reside in `paper/sections/` as separate files: introduction, related work, methodology, results, discussion, and conclusion.

## 9 — Related Work written (2026-08-22)

- `paper/sections/02_related_work.tex` is the first section converted from bullets to prose: 329 words, four paragraphs, no subsections. The user's constraint is **at most 350 words and only strictly needed citations**, because the four-page limit must leave room for Methodology and Results, and the literature is expanded implicitly elsewhere.
- Paragraph structure: (1) gaze/pupil as behavioural evidence and the ceiling of fixed feature sets; (2) learned representations — GazeMAE as gaze-specific, MOMENT as general-purpose frozen encoder, plus the open question of out-of-the-box transfer; (3) evaluation design — dependent within-subject windows, random splits, preprocessing fitted on the full dataset; (4) positioning as an empirical comparison extending the authors' earlier feature-based analyses, not a new encoder or fine-tuning method.
- Confirmed framing decisions: the two self-citations use **"our own prior work" / "our earlier feature-based analyses"** phrasing (user chose this over neutral third person). Related Work stays **neutral about the expected outcome** — it states the open question but does not foreshadow that frozen embeddings will underperform; the Results section must carry that argument alone.
- `paper/references.bib` holds six Crossref-verified entries: `bautista2020gazemae` (ICPR 2020 proceedings version, doi:10.1109/ICPR48806.2021.9412761), `goswami2024moment` (ICML 2024), `bozak2026workload` (AI 7(8):325, doi:10.3390/ai7080325), `bozak2024emotion` (IS 2024 SCAI, https://is.ijs.si/?p=16837), `kahneman1966pupil` (Science, doi:10.1126/science.154.3756.1583), `rosenblatt2024leakage` (Nat. Commun. 15:1829, doi:10.1038/s41467-024-46150-w).
- biblatex name form for suffixes is `Last, Jr., First` (`Naval, Jr., Prospero C.`); the BibTeX-style `Last, First, Jr.` renders incorrectly.
- Stale `paper/main.bbl-SAVE-ERROR` removed. Remaining bullet-only sections: introduction, methodology, results, conclusion.

## 10 — Dataset status: unpublished, collection ongoing (2026-08-22)

- The dataset behind this paper is **not published and still being collected**. This is therefore a **preliminary study**, and the manuscript says so explicitly in the Related Work positioning sentence.
- The data is **not the CLUES dataset** and has no data-level connection to it. An earlier note in this file wrongly claimed otherwise; that claim was retracted by the user on 2026-08-22 and the corresponding `clues2026dataset` stub was removed from `paper/references.bib`. Do not cite the IMWUT/CLUES paper as the data source.
- The only real relationship to CLUES is thematic: CLUES likewise pairs eye-tracking data with psychological labels, and the same team worked on both datasets. Any future mention must be framed as study similarity, never shared data, shared participants, or a shared collection.
- It is also **not** the same data collection as the 54-participant lab task battery in the AI 7(8):325 workload paper.
- Internal name in code and configs remains TrustMe (`trustme_root`, `TRUSTME_DATA_ROOT`); the manuscript's dataset name is still undecided and must be confirmed before Methodology prose is written.

## 11 — Performance-result audit (2026-08-22)

- The newest completed matching performance artifact found is the 15-subject run `TrustME-ET-end-to-end/results/tobii_experiments/20260514_150320/tables/protocol_summary.csv`. Its q5 `loso_normalized` rows use the intended centred-label rule, RF/MLP classifiers, and all four representations, but it predates the current 17-subject exports (July 2026) and is therefore not the final paper cohort result.
- `results/paper/table1_primary_loso_results_15_subject_snapshot.csv` is a paper-shaped, explicitly provisional extract of that run. Accuracy includes all 15 folds; balanced accuracy, macro-F1, and AUC use 14 folds because `s_004_pk` has a one-class q5 test fold. The majority baseline is common across representations/models.
- The protocol evaluator had labelled positive-class binary F1 as `macro_f1`. It now uses `average="macro"`, with a regression test. The provisional CSV's true macro-F1 values were recovered from saved fold-level sample counts, positive-class precision, and recall; no values were invented.
- Do not describe the 15-subject CSV as final. Before filling Table 1, freeze the paper cohort and run the q5-only `loso_normalized` protocol on the same aligned, coordinate-normalized cohort used by the manuscript. The current performance example points to the July all-window artifacts, while the normalized subject-tree exports contain 17 subjects; this input-path/cohort choice must be reconciled.

## 12 — Rerun scope and runtime estimate (2026-08-22)

- Coordinate normalization changes only raw gaze values. Across all 17 subjects, `tobii_features.csv`, `tobii_gazemae_embeddings.csv`, and `tobii_moment_embeddings.csv` are byte-identical between `tobii/` and `tobii_coordinate_normalized/`. For a frozen 15-subject cohort, only raw modelling needs retraining; the corrected paper extract can reuse saved predictions for the other representations.
- Moving from 15 to 17 subjects requires rerunning every representation, because the two new held-out folds are needed and adding subjects changes each LOSO training set and the fold-dependent engagement centring. Results cannot be updated by merely appending two folds.
- The 2026-05-14 RTX 4070 SUPER log records 23.0 minutes for q5 `loso_normalized` raw with MLP+RF and 83.1 minutes for all four representations with both models. Scaling from 121,176 windows/15 folds to the current 177,244-window/17-subject analysis cohort gives rough compute estimates of 38 minutes raw-only or 2.3 hours for all representations. Allow roughly 45–60 minutes and 2.5–3 hours respectively for loading/output overhead. The old full three-target/five-protocol grid took about 11 hours and would scale to roughly 18 hours; it is unnecessary for Table 1.

## 13 — Final 17-subject paper experiment locked (2026-08-22)

- User approved the current 17 named subjects, q5 only (paper name `engagement_level`), coordinate-normalized raw input, normalized LOSO only, RF+MLP, and one fixed seed (42). All other supervised/temporal/HMM protocols are disabled. A one-step persistence check is enabled once because it is inexpensive and is reported separately, never ranked against LOSO.
- `configs/final_paper_experiment.yaml` freezes the exact subject names and reads `ml/tobii_coordinate_normalized/` through `${TRUSTME_SUBJECT_ROOT}`. Newly arriving subject directories cannot silently enter the run. The common four-representation intersection has 157,685 windows; after requiring numeric q5, the final modelling cohort is exactly **140,531 windows across all 17 subjects**. Per-subject counts will be written to `cohort_window_counts.csv`.
- Final representation dimensions after alignment are raw 600 (five channels × 120 samples), handcrafted export 199 columns before fold-local metadata removal/filtering, GazeMAE 256, and MOMENT 1,024. The handcrafted structural-drop list now explicitly removes q1–q9, q5 itself, sleep feedback, prompt identifiers/times, window identifiers, subject/source fields, timestamps, and window length, preventing survey/prompt metadata leakage.
- The held-out-participant engagement centre now follows the manuscript literally: it is the unweighted mean of the training participants' individual q5 means, rather than the window-weighted global mean used by the older code. True macro-F1 and majority macro-F1/AUC are computed directly in the final run.
- Persistence is now true one-step last-observation prediction and is constrained within source recordings; it is executed once with `model=persistence`, `representation=label_history`, rather than redundantly for every representation/model pair.
- With `outputs.paper_only: true` and plotting disabled, each timestamped final result directory contains only the config snapshot and the needed CSVs: `paper_main_results.csv` (mean, sample SD, paired deltas, per-metric valid-fold counts), `loso_fold_metrics.csv`, `paper_persistence_result.csv`, `persistence_fold_metrics.csv`, and `cohort_window_counts.csv`.
- A full read-only production load passed: 140,531 aligned rows, 17 subjects, no missing q5, all four expected shapes. It took 1:52 and peaked at 3.75 GB RAM. The optimized NumPy raw vectorizer is regression-tested against the established pandas masking/interpolation semantics. The data-free suite passes 13 tests.

## 14 — Final 17-subject paper results completed (2026-08-23)

- The final run completed at `results/final_paper_experiment/20260823_101436/`. Its report-ready sources are `tables/paper_main_results.csv` for Table 1 and `tables/paper_persistence_result.csv` for the separate persistence check. Fold-level audit files and cohort counts are in the same `tables/` directory. `paper/sections/04_results.tex` now records these exact paths in source comments and explicitly warns against using the obsolete 15-subject snapshot.
- Validation passed 18 consistency checks: seed 42 and q5-only configuration; only normalized LOSO plus persistence enabled; the frozen 17-subject cohort totals 140,531 windows; all 136 expected LOSO rows (8 model–representation combinations x 17 subjects) completed without skipped folds; all summaries exactly reproduce fold-level means, sample SDs, paired deltas, and valid-fold counts; all 1,434 persistence temporal folds completed and the persistence summary exactly reproduces subject-macro aggregation.
- `s_004_pk` is the single one-class LOSO test subject, so accuracy has 17 valid folds while balanced accuracy, macro-F1, and ROC-AUC have 16. This is metric availability information only; the final performance values have not yet been interpreted in the manuscript.

## 15 — Results interpretation and dataset-context bullets added (2026-08-23)

- Interpreted `paper_main_results.csv`: ranking raw >= handcrafted > GazeMAE > MOMENT holds across accuracy, balanced accuracy, macro-F1, and AUC, but absolute discrimination is weak everywhere (best AUC .551) and between-subject variance (accuracy std .11-.29) dwarfs the between-representation deltas (.003-.017). MOMENT never beats chance (AUC ~.50); GazeMAE beats majority with RF but drops below chance with MLP, so it is flagged as model-dependent and **not** a settled negative result -- needs targeted follow-up before concluding gaze-specific embeddings fail here.
- User's explicit interpretation decisions: (1) no additional significance testing (no Wilcoxon or similar) will be added to back the ranking claim -- report effect sizes/deltas descriptively only; (2) the RF/MLP flip (MLP helps raw/handcrafted, hurts GazeMAE/MOMENT) is included only as one minor, tentative sentence, not emphasized; (3) practical recommendation nuance: raw is the strict empirical winner (best scores, least preprocessing/compute/storage) but the paper still recommends at least trying handcrafted features for similar tasks, based on prior experience/literature -- this single preliminary dataset should not be read as ruling handcrafted features out.
- Resolved: the one-step persistence result (accuracy .999, balanced accuracy .960, macro-F1 .965) is largely a labeling artifact, not evidence of genuine engagement stability. Each ESM answer is broadcast unchanged to every 3-second window until the next prompt (`prompt_id`/`prompt_time` are dropped as leakage-prone metadata precisely because of this); empirically 88.4% of the 90-window persistence test chunks (`persistence_fold_metrics.csv`, `skip_reason=one_class_test`) never see the label change at all. Decision: do NOT use persistence to explain "why cross-subject generalization is hard" in Discussion -- that would borrow credibility from a mostly-mechanical number. `paper/sections/04_results.tex` now carries a one-sentence caveat that the near-ceiling result reflects the label-broadcast windowing scheme and is a sanity check only; the persistence bullet was removed entirely from the Discussion part of `paper/sections/05_conclusion.tex` (kept in Results only, per user's explicit choice).
- Found the actual ESM questionnaire text at `/home/ppg/eyetracking/trust-me/src/development/polling_ui/questions.json` (sibling `trust-me` repo, not this one). Order maps to q1-q9: q1 valence, q2 arousal, q3 "Right now I feel bursting with energy", q4 "Right now I feel enthusiastic about my job", q5 "Right now I am immersed in my work" (exact wording -- supersedes the earlier paraphrase "How immersed in your work did you feel just now?" used in this file and in prior manuscript drafts), q6 fatigue/battery item, q7 physical discomfort, q8 workplace interpersonal conflict, q9 stress; sleep questions and productivity are separate, non-`q`-numbered items. q3-q5 read as a momentary 3-item work-engagement subscale (vigor/dedication/absorption), plausibly adapted from the UWES-3 (Schaufeli et al.) -- not yet confirmed with the user or cited.
- `paper/sections/03_methodology.tex` now has bullets on: TrustMe project (JSI + Universit\`a della Svizzera italiana), `https://dis.ijs.si/trust-me/`, multimodal collection (RGB camera, eye tracker, microphone, activity-watch/screen-activity; this paper uses eye-tracking only), full-workday recordings repeated over ~3 weeks, ESM protocol with multiple daily prompts, and the still-being-collected/not-yet-public/preliminary-study status. `paper/sections/04_results.tex` and `paper/sections/05_conclusion.tex` got the GazeMAE-model-dependence flag, the minor RF/MLP observation, and the practical-recommendation nuance described above.

## 16 — Full manuscript written (2026-08-23)

- All bullet-only sections are now prose. `paper/main.pdf` compiles to exactly **4 pages** including references, with zero overfull boxes. Sections: 1 Introduction, 2 Related Work (unchanged), 3 Methodology (3.1 Data and Eye-Tracking Representations, 3.2 Experimental Design), 4 Results, 5 Discussion, 6 Conclusion. Abstract written (~150 words).
- User decisions taken during writing (2026-08-23): authors are **Tomi Božak and Gašper Slapničar, Jožef Stefan Institute, Ljubljana** (e-mails `tomi.bozak@ijs.si` — inferred from the JSI pattern, needs verification — and `gasper.slapnicar@ijs.si`, confirmed from the sibling `trust-me` repo README); the dataset is **not named** in the manuscript and the project URL is omitted (described only as "an ongoing multimodal study in which office workers are recorded during ordinary work"); `\begin{acks}` holds a red `\placeholder` for funding/grant numbers.
- New dataset facts established from the raw tree and used in Methodology: Tobii sampling **60 Hz**; windows are **3 s with a 2.25 s step (25 % overlap)**; **each ESM prompt labels the 15 minutes of recording that precede it** (verified from `labeled_windows_3sec.csv`: prompt_time minus window start_time is uniform on 0–900 s, max 400–401 windows per prompt). This *supersedes* the earlier note that answers are broadcast "until the next prompt fires"; the persistence artifact conclusion is unaffected, since labels are still constant inside each 15-minute block.
- Cohort chain: ~**7.26 M** candidate 3-s Tobii windows across **657 full-workday recordings** (19–79 per participant, median 32) → labelled windows with handcrafted features **177,244** → after GazeMAE/MOMENT QC **157,685** → with a numeric q5 rating **140,531** (≈2 % of recorded windows). q5 scale is **0–6**; cohort distribution 0.5/3.0/6.3/17.4/28.0/34.7/10.0 %; participant means span **2.22–4.93**; after within-participant centring the positive rate is **56.3 %** overall and 33–72 % per participant. `s_004_pk` is the one-class fold because its ratings (mean 2.39, max 4.0) all fall below the training centre.
- Handcrafted pipeline numbers: 199 exported columns, 18 structurally dropped, 181 numeric candidates, **58–60 selected per LOSO fold** (recomputed with `FeatureFoldPreprocessor` on the exact 140,531-window cohort; 14 zero-variance and 108 correlated columns dropped in the all-subject fit). GazeMAE = 128 pos + 128 vel = 256-d; MOMENT-1-large = 1,024-d; raw = 5 × 120 = 600-d. Raw windows longer than 120 samples are truncated and shorter windows are padded with their final value; the final cohort contains 79,402 longer, 58,032 shorter, and 3,097 exact-length windows (`Win_len` range 32–182). Display resolutions span 1920×1080–3840×2160 and normalization is **per source recording**. In the final four-representation intersection, only `Francesco_Bomba` has two resolutions: 3 source recordings and 1,713 windows at 3840×2160, plus 17 source recordings and 11,075 windows at 1920×1080. The wider raw archive also assigns two allowed resolutions to `s_002_th` and `s_003_za`, but their final-cohort recordings use only 1920×1200 and 2560×1440, respectively. **94.7 %** of gaze samples fall inside `[0,1]^2`.
- New file `tobii_experiments/make_paper_figures.py` regenerates both manuscript figures from the config + the descriptive-analysis projections: **Figure 1** (single column) = gaze-density map + engagement-rating distribution with per-participant mean ticks; **Figure 2** (double column) = 2×4 t-SNE grid, top row coloured by rating, bottom row by participant. `.gitignore` gained `!paper/figures/*.pdf` so the figure PDF is tracked despite the blanket `*.pdf` rule.
- Interpretation follows the earlier ground rules: no significance tests, ranking reported as a descriptive tendency, GazeMAE's classifier-dependent result flagged as open, persistence kept in Results only with the labelling-artifact caveat, and the practical recommendation keeps the "still worth trying handcrafted features" nuance.

## 17 — TeX semantic line formatting (2026-08-23)

- Manuscript source under `paper/` uses one complete sentence per physical line; this is a whitespace-only source-formatting convention and does not change the rendered manuscript.
- Workspace VS Code settings enable `editor.wordWrap` so long semantic lines remain visually wrapped while editing.

## 18 — Manuscript style preference (2026-08-25)

- Keep the manuscript's claims, numbers, citations and interpretation unchanged while making the prose sound more natural.
- Avoid sentence-level `---` asides, semicolons and non-enumerative colons where a direct sentence works better.
- Avoid conspicuous template-like contrasts such as “rather than”, “not only ... but also”, and “not X but Y”; retain colons for genuine lists and TeX dashes for numeric ranges where appropriate.

## 19 — Repetition policy (2026-08-25)

- Emphasise the evidence gap once in the Introduction, then let Related Work establish its literature basis without restating the same motivation.
- Keep cross-section recap functional: Results reports observations, Discussion interprets them, and Conclusion gives only the shortest practical summary.
- Remove repeated statements within a paragraph or adjacent paragraphs when the second occurrence adds no new evidence, qualification or implication.

## 20 — Inline author comments resolved; manuscript publish-ready (2026-08-26)

- All bracketed `(...)` review comments the user left in `paper/sections/*.tex` are resolved and removed. The build is back to exactly **4 pages, zero overfull boxes, no undefined references**.
- Three verified references were added to `paper/references.bib`: `middlehurst2024bakeoff` (DMKD 38(4):1958–2031, doi:10.1007/s10618-024-01022-1), `bixler2016mindwandering` (UMUAI 26(1):33–68, doi:10.1007/s11257-015-9167-1), `fridman2018cognitive` (CHI 2018, doi:10.1145/3173574.3174226). They fill the three `(cite)` markers: representation choice matters in time-series classification (Introduction), handcrafted ocular features as the applied default besides our own work (Introduction + Related Work), and optimistic splits inflating results (Introduction, reusing `rosenblatt2024leakage`). The Bixler title carries `\hspace{0pt}` after the hyphens in "Gaze-Based User-Independent" so the reference list does not overflow.
- Handcrafted-feature paragraph rewritten: it now explains *how* the features are built (tracker event classification for blinks/fixations/saccades, then counts, rates, durations, fixation location and dispersion, saccade amplitude and velocity, pupil size and velocity, eye–screen distance, per-channel missingness) and states only that identifiers, timestamps and questionnaire columns are not used as model input. The earlier "199 columns, 18 removed" framing is gone. Feature groups verified against `TrustME-ET-end-to-end/data_processing/handcrafted_features_calculation/` and a real `tobii_features.csv` header.
- Terminology aligned: "majority baseline" everywhere (was "most-frequent baseline" in Methodology), "ROC-AUC" in prose with `AUC` only in the table (caption now says so), and the representation is labelled "Raw signals" in Methodology to match the table and the rest of the prose.
- Figure 2 caption no longer says the projections "should not be interpreted"; it now states what is visible (participant patches in raw, near-uniform MOMENT, no rating structure). The interpretation moved to the Results text: raw forms participant-dominated patches, handcrafted/GazeMAE group weakly, MOMENT is near-uniform, most exposed structure is person-specific, and the frozen encoders appear to smooth it away — explicitly flagged as a conjecture because the projections are exploratory.
- Discussion/Conclusion wording de-"bookified" per the user's style note (no "best read as", "give little reason to", "did not repay their cost"); fine-tuning is now mentioned once instead of three times.
- **Figure 1 recoloured to blue–red.** `tobii_experiments/make_paper_figures.py` gained module-level `DENSITY_CMAP` (a `LinearSegmentedColormap` with stops `#f7fbff → #c6dbef → #6baed6 → #2166ac → #b2182b → #67001f` at 0/.06/.15/.30/.50/1.0), `BAR_COLOUR = "#2166ac"` and `MEAN_TICK_COLOUR = "#b2182b"`, so panel (b)'s bars use the same blue and the participant-mean ticks the same red as the density map. `paper/figures/fig1_descriptive.pdf` was regenerated from the real 17-subject data (cohort 140,531 windows, 94.7 % on-screen samples reproduced exactly).
- Raw data root for regenerating figures is `/home/ppg/eyetracking/TrustME-ET/data/raw/TrustMe` (set `TRUSTME_SUBJECT_ROOT` to it; `/mnt/trustme_users` is empty). Use the `trust-me-et` env interpreter at `/home/ppg/miniconda3/envs/trust-me-et/bin/python`. The gaze-histogram scan over all subjects takes a few minutes, so cache `labels`/`hist` when iterating on figure styling.
- Still open by the user's explicit choice: the red `\placeholder` for funding and grant numbers in `\begin{acks}` stays until submission.

## 21 — Full scientific and language audit (2026-08-26)

- The full manuscript was checked against the final experiment configuration, fold-level and summary results, representation-generation code, primary GazeMAE and MOMENT sources, and the current IS2026 author instructions. The prose remains close to the user's original style and avoids new semicolons, rhetorical colons, and sentence-level dash asides.
- Corrected material claim inconsistencies: raw uses five selected channels and truncates or pads to 120 samples rather than retaining everything measured; the held-out engagement threshold is the unweighted mean of training-participant means rather than the held-out participant's own mean; the earlier 56.3% and 33--72% positive-rate sentence described a diagnostic full-cohort centring and was removed; raw leads all reported metrics, handcrafted ranks second on balanced accuracy, macro-F1 and ROC-AUC, and GazeMAE has the second-highest accuracy; MOMENT is below the accuracy baseline but not below the baseline on every metric; its AUC is described as close to chance without an unsupported significance claim.
- Coordinate normalisation is now described faithfully. It applies only to the raw export. Handcrafted features, GazeMAE embeddings, and MOMENT embeddings were computed earlier from the original pixel-coordinate exports and copied unchanged. This asymmetry is also stated as a limitation.
- Persistence reporting now reflects subject-macro aggregation: 1,434 temporal blocks, participant-averaged accuracy over 17 participants, and balanced accuracy/macro-F1 over the 13 participants with two-class test blocks. The effective-sample-size limitation from overlapping windows and repeated 15-minute labels is explicit.
- Figure 2's earlier "common sample of 5,000" caption was false: raw/handcrafted projections had 5,000 rows and the embedding projections 4,430. `analysis.data.load_representation_ids` and the analysis runner now form the four-representation ID intersection before sampling. All four regenerated t-SNE CSVs contain the same 5,000 unique IDs, and `paper/figures/fig2_tsne.png` was regenerated.
- The new AI disclosure names the tools precisely as OpenAI Codex with GPT-5.6 Sol and Anthropic Claude Code with Claude Opus 5. The MOMENT reference now uses the report-ready PMLR page range 16115--16152 and proceedings URL.
- **The manuscript is not yet submission ready.** Current IS2026 ethics instructions require human-subject permissions and informed consent to be stated. No verified ethics details exist in this repository or the sibling project, so `paper/sections/03_methodology.tex` contains a red placeholder for the ethics approval or exemption and informed-consent statement. The authors must replace it with verified wording. The IS2026 instructions also ask authors to check academic titles, affiliations, and contact email addresses, so those details need a final author check. The DOI remains empty for initial submission and must be added to `\acmDOI` in the camera-ready version when supplied by the chairs.
- After the audit and shared-sample figure regeneration, the clean build is still exactly four A4 pages with no overfull boxes, undefined citations, or undefined references. The full data-free test suite passes (`13 passed`).

## 22 — Gašper PDF-review handoff (2026-08-28)

- `paper/main_GS.pdf` contains 39 substantive highlight comments by Gašper across pages 1--3 and none on page 4.
- `paper/GASPER_REVIEW_COMMENTS.md` transcribes all 39 comments, identifies each selected passage and its TeX source location, and provides a blank `Tomi / instruction to Claude Code` field for every item. The entries follow manuscript source order rather than PDF annotation-creation order.
- No mentor comment has been applied to the manuscript. Claude Code is instructed in the handoff not to implement an item until Tomi adds an explicit decision to its response field.

## 23 — Gašper's 39 review comments implemented (2026-08-28)

- All 39 comments from `paper/GASPER_REVIEW_COMMENTS.md` are resolved: 35 implemented, 2 needed no change (25, 38), 2 were verification tasks (30, 31). Per-comment outcomes are recorded in an `Implemented (2026-08-28)` line under every entry in that file.
- **Primary metric changed from accuracy to balanced accuracy** (comment 12, decided with the user). The stated reason in the paper is class imbalance, not the wider margin: the positive rate is 33–72 % per participant, so plain accuracy partly measures each person's base rate, while balanced accuracy has a fixed 0.500 chance level. Headline is now **0.536 balanced accuracy, +0.036 over chance** (raw + MLP) instead of 0.538 accuracy / +0.014, and the ranking becomes monotonic raw > handcrafted > GazeMAE > MOMENT. Accuracy and its paired delta remain secondary columns. Balanced accuracy uses 16 folds (s_004_pk is single-class), accuracy 17 — stated in the caption and in Experimental Design. Consistent with the no-added-significance-tests rule: no inferential statistics were introduced.
- **Terminology unified.** "Absorption" is gone from the paper. Two terms with one job each: **engagement** is the construct and the running term (title, keywords, framing, and the variable name *engagement rating*); **immersion** names the specific ESM item, "Right now I am immersed in my work", introduced once in Methodology. Also unified: encoders are *pretrained* (a property of the checkpoints), used *frozen* (our choice); `fold-local` → `per-fold`; `office workers` → `knowledge workers` (only the setting is still called "office"); `unseen-participant` → `left-out participant`.
- **The 30 % / 32-frame window rule is two non-redundant conditions.** They bind in different regimes because window length varies (`Win_len` 32–182; nominal 3 s at 60 Hz = 180). The 30 % rule is proportional and binds on long windows (54 frames at 180); the 32-frame rule is absolute and binds on short ones (30 % of 60 is only 18). Crossover ≈ 107 frames (~1.8 s). Source of truth is the upstream export config, not this repo: `TrustME-ET-end-to-end/configs/processing.yaml` (`min_valid_fraction: 0.30`, `min_valid_frames: 32`), enforced in `data_processing/moment_embedding_calculation/segment_trustme.py:212-232` and `data_processing/gazemae_embedding_calculation/build_gazemae_embeddings.py:344-350`. Note the interpolation gap limit (`max_invalid_frames: 60`) is **not** strictly enforced in the MOMENT path, which does `ffill().bfill()` after the limited interpolate — so do not claim a hard 1-second gap bound in the paper.
- **Figure 1's gaze density map was audited and is correct** (comment 30). `tobii_experiments/make_paper_figures.py` bins normalised coordinates, drops NaNs, keeps only `[0,1)²`, and the `density.T` + `origin="upper"` + `extent=(0,1,1,0)` combination puts y=0 at the top of the screen, matching the axis labels. No transpose or flip bug; the central concentration is genuine. This was a code-path audit only — the numbers could not be recomputed because `$TRUSTME_SUBJECT_ROOT` was unset and the subject tree was not mounted.
- New reference added: `rayner1998reading` (Rayner 1998, Psychological Bulletin 124(3):372–422, doi:10.1037/0033-2909.124.3.372), Crossref-verified. It serves both comment 17 (attention/processing-load citation in the Introduction) and the fixation-duration justification for the permissive validity threshold.
- **Table 1 stays one column wide.** Gašper's comment 35 asked for full text width and it was built that way as a `table*`, but the user overruled it the same day: Table 1 must be **column width, not page width**. It is now a single-column `table` stretched to fill `\columnwidth` via `tabular*` with `@{\extracolsep{\fill}}`. To fit one column it uses combined `Representation, Model` row labels (`Raw, RF`, `Raw, MLP`, ...) instead of the four `\multicolumn` group-header rows, with the per-representation dimensionality moved into the caption. Column order leads with the primary metric (Bal. acc. | SD | M-F1 | AUC | Acc. | Δ). `tabularx` is no longer used and the package was removed again from `main.tex`.
- **Page budget.** The new text cost about one page. Recovered without losing any result, number, limitation or citation: redundant sentences merged or cut across all five sections, the four table group-header rows replaced by combined row labels, the table caption shortened, the table body set in `\footnotesize`, and Figure 2 scaled to `0.86\textwidth`. **`0.86\textwidth` is the largest figure width that fits** — anything added to the prose from here will push the paper to 5 pages again. Final build is exactly 4 A4 pages, no errors, only sub-10 pt overfull hboxes in bibliography entries with long DOIs. Note that float placement is non-monotonic here: a *smaller* Figure 2 (0.80) can produce a worse packing than 0.86, so always re-measure rather than assuming smaller is safer. Also let `latexmk` converge (run it until it reports up-to-date) before trusting a page count; single-pass builds after `latexmk -C` report spurious 5-page results.
- Gašper's second affiliation (Jožef Stefan International Postgraduate School) was added, and a whole-paper tense pass applied on one rule: Introduction and Related Work in the present (framing, and what published work shows), Methodology and Results in the past (what we did and observed), atemporal facts in the present.

## 24 — Fixed-duration window wording clarified (2026-08-28)

- Methodology now states explicitly that every analysis window has a fixed three-second duration and that sampling gaps can leave fewer than the nominal 180 recorded frames. It no longer describes windows as longer or shorter. The two validity conditions remain unchanged at 30\,% valid recorded frames and at least 32 valid frames, followed by interpolation within retained windows.

## 25 — Final publication-readiness audit (2026-08-28)

- The paper was rebuilt to convergence and every rendered page was visually inspected. It remains exactly four A4 pages, with zero overfull boxes, undefined citations/references, visible placeholders, clipping or figure/table defects. The current full-width Figure 2 fits; the earlier 0.86-text-width ceiling is no longer current after the latest prose reductions.
- Repetition was reduced conservatively. The Introduction now names the representation choices without repeating the literature detail supplied by Related Work, and the practical paragraph in Discussion no longer restates the full result ranking. Abstract, Results, Discussion and Conclusion retain only their section-specific summary functions.
- A material class-balance inconsistency was corrected from the final fold artifact. Under the actual left-out-participant threshold, the fold-defined positive rate is **57.9 % overall** and **0–95.4 % across participants** (reported as 0–95 %), not the diagnostic 56.3 % and 33–72 % values. The latter describe within-participant centring and must not be used to characterise the held-out test folds. The leading raw-MLP variability comparison is now explicit: SD 0.047 exceeds its 0.036 chance margin and the 0.015 gap to the best handcrafted result.
- Four margin overflows found in the pre-audit build were removed through local wording changes. The remaining log messages are expected template warnings from suppressing ACM-only CCS/reference blocks and harmless underfull/column-balancing notices.
- **Submission is still blocked on author-supplied ethics details.** The official IS2026 human-subject policy says the paper must state the necessary permissions/compliance and that consent was obtained. No verified approval/exemption wording, identifier or consent detail exists locally, so none was invented. The official 2026 author instructions also require the authors to verify academic titles, affiliations and contact emails. The empty DOI is correct for initial submission and should be filled only for the camera-ready version when the chairs provide it.

## 26 — Ethics statement waived by the IS board (2026-08-29)

- The user consulted the IS board directly and was told that an ethics statement is not required for this paper. This author-confirmed, case-specific guidance supersedes the general-policy concern recorded in items 21 and 25.
- Do not add an ethics approval, exemption or informed-consent statement, and do not add an ethics placeholder. A full source search confirmed that the manuscript currently contains no ethics wording or incomplete placeholder.

## 27 — Citations restored and positive-window count verified (2026-08-29)

- Existing citations were restored at appropriate claims in the Introduction without adding bibliography entries: Kahneman/Rayner support eye tracking as a plausible sensor, the four applied feature papers support the handcrafted-feature claim, and the primary GazeMAE/MOMENT papers appear at the models' first main-text mention. The same primary papers now support the specific pretraining-domain claim in Discussion. Abstract citations remain omitted by convention.
- The final 17-fold artifact was audited across every representation/model row. Each of the 140,531 windows appears once as a held-out test window; exactly **81,341 are positive**, or **57.881179 %** (reported as 57.9 %), with participant rates **0–95.358841 %** (reported as 0–95 %). Test counts and rates agree across all representation/model rows, and the regression test for the held-out centring rule passes.
- The older **56.3 %** figure was produced by the descriptive full-cohort transform, which centres each participant using that participant's own labels. It must not be reported as the class balance of the leakage-safe LOSO evaluation, where the held-out participant is centred by the unweighted mean of the training-participant means.

## 28 — Final submission check and IDE build race (2026-08-29)

- The current manuscript compiles to a valid four-page A4 PDF with zero overfull boxes, unresolved citations/references, visible placeholders, clipping or layout defects. All four final pages were visually inspected.
- A concurrent IDE/LaTeX Workshop auto-build briefly truncated paper/main.pdf while the command-line check was running, indicated by paper/main.synctex(busy). After the IDE build finished, pdfinfo validated the final 2,791,394-byte PDF as four A4 pages. Before uploading, wait for any main.synctex(busy) file to disappear and open the exact PDF being submitted.
- The only remaining non-technical check is author metadata: the current PDF shows both names, affiliations and emails, but the IS author instructions ask authors to include/verify academic titles as well. The DOI remains correctly blank for initial submission.

## 28 — Figure 1 caption spacing checked (2026-08-29)

- The apparently large gap below Figure 1 is the standard `\textfloatsep` after a top-of-column float: 20 pt plus 2 pt minus 4 pt, inherited by the supplied `sigconf` `acmart` class. There is no manual `\vspace`, blank paragraph, or excess whitespace in the figure PDF causing it.
- The official IS2026 instructions ask authors to adhere to the supplied template, and the repository's `paper/acmart.cls` is byte-identical to the class originally imported with that template. The spacing was therefore left unchanged rather than overridden locally.

## 29 — MLP first-layer width verified (2026-08-29)

- The manuscript's MLP first-layer width of **1,028** is not a transcription error: the completed final run's immutable `config_snapshot.yaml` records `hidden_dims: [1028, 512, 256, 128, 64]`, and the model implementation passes these values directly to PyTorch `nn.Linear` layers. Although 1,028 may have originated as a configuration typo for 1,024, changing the manuscript to 1,024 would misreport the executed experiment and would require a rerun. The paper and frozen final configuration therefore remain unchanged.

## 30 — GazeMAE visual-angle rescaling and raw full-window resampling (2026-08-29)

- Two defects were found in the representation pipeline and fixed, then the LOSO experiment was rerun (`results/final_paper_experiment/20260829_150833`).
- **Raw truncation.** `configs/final_paper_experiment.yaml` used `raw.length_mode: truncate_pad` with `flatten_length: 120`, so `clean_and_resize_channel` took `cleaned[:120]` — the first 120 recorded rows of a 3 s / 180-sample window. Measured on `s_001_gs`: 41 % of windows were truncated, 18 % lost a third of their span, 14 % of all samples were discarded, and the remaining 59 % (fewer than 120 rows) were padded with their final value repeated. The unused `resample_length: 120` sitting in the same block indicates the truncation was accidental. Fixed by setting `length_mode: resample`; output stays 600-dimensional but now covers the full 3 s.
- **GazeMAE coordinate scale.** `build_gazemae_embeddings.py` fed raw display pixels to the encoder, but upstream GazeMAE (`gazemae/data/corpus.py`, `settings.py PX_PER_DVA = 35`) rescales so 1° of visual angle spans 35 px. Across the real cohort the pre-scale `px_per_dva` ran **22.6–130.0 (a 5.8× spread)**, varying with display resolution and viewing distance, i.e. a per-participant distortion. Fixed in the canonical builder at `TrustME-ET-end-to-end/data_processing/gazemae_embedding_calculation/build_gazemae_embeddings.py` (the copy in `gazemae/trustME/` is a stale sibling and was left untouched). Post-fix `px_per_dva × dva_scale == 35.0` for all 157,685 windows.
- Physical display sizes are **not recoverable from the data**: every TSV header reports the same PyGaze default `display size in cm: 33.8x27.1`, inconsistent even with the 3440×1440 aspect ratio. The user chose to assume standard monitor pairings per resolution (1920×1080→24″, 1920×1200→24″ 16:10, 2560×1440→27″, 3440×1440→34″ UW, 3840×2160→27″), combined with the real per-window `AverageDistance` (in **cm**). The assumption is stated in the manuscript.
- **MOMENT needed no change**: `momentfm/models/moment.py` applies RevIN (per-sample, per-channel standardisation) to every input, and `segment_trustme.py` deliberately leaves external normalisation off for that reason. Its results are byte-identical before and after, as are the handcrafted-feature results and the whole 140,531-window cohort — a useful confirmation that only the two intended things moved.
- **Do not run `export_ml_csv.py` for this cohort.** It rewrites all four representation CSVs per subject, but this repo's handcrafted CSV and MOMENT npz are s_001_gs-only remnants; a full export would destroy the complete 17-subject features and MOMENT CSVs. Use the new `data_processing/export_gazemae_csv_only.py` instead (regression-tested to reproduce an existing CSV exactly: 273 identical columns, same rows and `window_uid`s). Backup of the pre-rerun state: `/home/ppg/eyetracking/_gazemae_rebuild_backup_20260829`.
- **Result changes.** The 0.536 headline balanced accuracy survives but now belongs to raw + RF (raw + MLP is 0.534 and leads on macro-F1 0.472, AUC 0.553, accuracy 0.542). GazeMAE is **no longer below chance** (MLP 0.493→0.505 balanced accuracy, AUC 0.494→0.508, Δ −.004→+.009), so the "result depended on the classifier" observation was removed from Discussion, and the false claim that "the MLP improved raw and handcrafted inputs but lowered both embedding results" was replaced. Every headline conclusion is unchanged.
- **Outstanding:** `paper/figures/fig2_tsne.png` and `results/data_analysis_q5/raw_q5/tsne/projection_gazemae_embeddings.csv` predate the rebuild, so the two GazeMAE t-SNE panels still show the old embeddings. Figure 1 is unaffected (raw samples and the cohort are unchanged).


## 31 — Figure 2 regenerated; participant structure quantified (2026-08-29)

- The t-SNE projections and both manuscript figures were rebuilt after the GazeMAE rescaling. Deterministic check: `projection_raw.csv`, `projection_handcrafted_features.csv` and `projection_moment_embeddings.csv` came back **byte-identical** to the pre-rebuild versions (seed 42, unchanged inputs); only `projection_gazemae_embeddings.csv` changed. Figure 1 also regenerated identically (140,531 windows, 17 participants, 94.7 % on-screen).
- Participant identifiability from the 2-D map (25-NN, 5-fold CV, chance 0.059), new vs old: Raw **0.429 / 0.429**, Handcrafted **0.336 / 0.336**, GazeMAE **0.249 / 0.324**, MOMENT **0.195 / 0.195**. The rescaling removed a per-participant display-resolution factor that the encoder had been representing, so GazeMAE's participant structure dropped and now sits between handcrafted and MOMENT.
- Rating structure remains absent in every projection: 25-NN accuracy for the 0–6 rating is 0.360 (raw), 0.321, 0.314, 0.303 against a 0.342 majority-class baseline — i.e. at or below baseline everywhere. Figure 2's caption claim is therefore still correct for the new GazeMAE panel.
- Both Results sentences about Figure 2 were left unchanged because they remain accurate: raw still shows the clearest patches, handcrafted and GazeMAE group more weakly, MOMENT is closest to uniform. The one sentence that could now be sharpened is the conjecture that "the weaker grouping in the frozen embeddings may reflect smoothing by the encoders" — the measured drop after rescaling suggests part of the earlier GazeMAE grouping was display-scale, not smoothing. Left as is on page-budget grounds; the manuscript never describes the pre-fix state, so introducing it would need explanation that does not fit.
- Final PDF: 4 A4 pages, 0 overfull boxes >20 pt, 0 undefined references or citations.

## 32 — Final consistency pass (2026-08-29)

- Every number in the manuscript was machine-checked against `results/final_paper_experiment/20260829_150833`: all 8 Table 1 rows × 6 columns reproduce the CSV exactly in the rendered PDF, and the prose figures (0.536, 0.036, 0.534, 0.472, 0.553, 0.542, 0.521, 0.510, 0.505, 0.075, the −0.003…+0.036 range, 140,531, 2,136–25,447, 17 participants, persistence 0.999/0.960/0.965 over 1,434 blocks and 13 subjects) all match. All 10 references are cited and all 10 in-text citation numbers resolve; Table 1 and both figures are referenced; no placeholders or unresolved refs.
- **A factual error introduced during the rerun edits was caught and fixed.** The replacement sentence "Both classifiers ordered the representations identically" is false: on balanced accuracy the random forest gives raw > GazeMAE > handcrafted > MOMENT while the MLP gives raw > handcrafted > GazeMAE > MOMENT, and the orders also differ on macro-F1 and plain accuracy (they agree only on ROC-AUC). Replaced with the claim that actually holds: "On balanced accuracy both classifiers placed raw first and MOMENT last, while the two middle representations swapped." Within-representation RF/MLP gaps are 0.002–0.013, all below the 0.075 leading SD.
- Two further consistency fixes: the coordinate-convention sentence omitted MOMENT (now "the handcrafted features and MOMENT embeddings retain pixel coordinates", with the redundant "before display-coordinate normalisation" dropped from item 4), and Table 1's caption promised "Best value per column in bold" although the SD column carries no bold (now "per metric column"). The `\cite{bozak2024emotion,bozak2026workload}` group that rendered as "[4, 3]" was reordered to "[3, 4]".
- Known and deliberately left: the stated "0.015 gap to the best handcrafted result" is the difference of the rounded table values (.536 − .521); at full precision it is 0.0156. This rounding convention predates the rerun and is what a reader verifies from Table 1, so it was not changed.
- Final state: 4 A4 pages, 0 overfull boxes >20 pt, 0 undefined references or citations, no `synctex(busy)` file, all four pages visually inspected.

## 33 — Handcrafted event provenance corrected (2026-08-30)

- The manuscript's earlier claim that blink, fixation and saccade labels came from the tracker's event classification was false. The feature pipeline derives them algorithmically offline from tracker signals; this is not manual human classification.
- Blinks are inferred from 90--400-ms missing-gaze intervals, fixations use I-DT with a 100-pixel dispersion threshold and 100-ms minimum duration, and saccades are non-blink intervals shorter than 400 ms between successive fixations. The extractor drops an input `Event` column if present.
- Missingness features are counts and fractions for selected signals, not every recorded channel. `paper/sections/03_methodology.tex` now states the offline event derivation and missingness scope accurately; the exact detector thresholds are retained here and in the extractor rather than expanded in the four-page manuscript.

## 34 — Handcrafted citations and offline wording (2026-08-30)

- The handcrafted-feature description now cites both earlier feature sets as `\cite{bozak2026workload,bozak2024emotion}`, which renders in ascending numerical order as [3, 4]. It explicitly begins the event-method sentence with "Offline detection" and keeps the compact wording required for the four-page limit.
- A transient malformed/missing `main.bcf` was caused by the documented LaTeX Workshop concurrent-build race (`main.synctex(busy)`), not by the citation command. After the IDE build finished, Biber and LaTeX completed normally: four A4 pages, no undefined citations/references or overfull boxes.

## 35 — Table delta aligned with the primary metric (2026-08-30)

- Table 1's $\Delta$ column now reports the fold-paired improvement in balanced accuracy over the majority baseline, matching the primary metric, rather than the previously reported plain-accuracy delta. The source is `balanced_accuracy_delta_mean` in `results/final_paper_experiment/20260829_150833/tables/paper_main_results.csv` (16 valid folds).
- Rounded deltas are raw RF +.036, raw MLP +.034, handcrafted RF +.008, handcrafted MLP +.021, GazeMAE RF +.010, GazeMAE MLP +.005, MOMENT RF +.001 and MOMENT MLP −.003. Raw RF is bold as the best delta.
- The $\Delta$ column is positioned immediately after balanced accuracy and before SD so that the primary score and its paired improvement are read together.

## 36 — Discussion frames the prediction difficulty as real-world (2026-08-30)

- The interpretation of the near-chance results now emphasizes that the gaze windows were collected during unconstrained, real-world office work. It does not imply that the three-second window length itself is the cause of the weak cross-person predictability.
